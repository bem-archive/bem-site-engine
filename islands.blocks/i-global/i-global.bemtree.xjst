this._mode === '', !this.data {

    true: {
        this.data = this.ctx;
        return applyNext(
            this._mode = '',
            this.ctx = [
                { block : 'i-global' },
                { block : 'b-page' }
            ]
        );
    }

    this._.isArray(this.ctx): {
        return applyNext(this.ctx = this.ctx[0]);
    }

}

block i-global {

    default: false

}
