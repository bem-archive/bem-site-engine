block user, elem icon {

    icon: {
        var glob = this['i-global'],
            login = glob.login;

        if(!login)
            return;

        return glob['center-host'] + '/user/avatar/' + login;
    }

}
