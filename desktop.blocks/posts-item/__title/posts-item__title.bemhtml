block posts-item, elem title {
    tag: 'h3'

    content: {
        var content = this.ctx.content,
            params = this.ctx.params

        return {
            block: 'link',
            url: '/' + params.lang + '/' + params.type + '/' + content.id,
            content: content.title
        }

    }
}
