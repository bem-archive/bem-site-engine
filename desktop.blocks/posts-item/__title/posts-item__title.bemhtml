block posts-item, elem title {
    tag: 'h3'

    content: {
        return {
            block: 'link',
            url: '/' + this.ctx.type + '/' + this.ctx.id,
            content: this.ctx.title
        }

    }
}
