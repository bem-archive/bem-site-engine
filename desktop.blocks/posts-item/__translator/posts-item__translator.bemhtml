block posts-item, elem translator {

    default, !(this.ctx && this.ctx.content && this.ctx.content.length > 0): {
        return false;
    }

    content: [
        BEM.I18N('posts-item', this.elem),
        ': ',
        applyNext()
    ]
}
