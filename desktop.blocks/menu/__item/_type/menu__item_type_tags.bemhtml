block menu, elem item, elemMod type tags {

    content: [
        {
            block: 'link',
            url: '/tags/' + this.ctx.id,
            content: this.ctx.id.toLowerCase()
        }
    ]
}

