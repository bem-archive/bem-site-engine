block menu, elem item, elemMod type libs {

    content: {

        return [
            {
                block: 'post',
                mods: {view: 'menu-item'},
                id: this.ctx.id,
                category: this.ctx.category
            }
        ]
    }
}
