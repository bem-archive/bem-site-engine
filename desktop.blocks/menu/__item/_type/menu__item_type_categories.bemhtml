block menu, elem item, elemMod type categories {

    content: [
        {
            block: 'link',
            url: this.elemMods['active'] == 'yes' ? null : this.ctx.url,
            content: this.ctx.name
        }
    ]
}
