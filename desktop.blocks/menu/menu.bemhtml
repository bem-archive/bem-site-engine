block menu {

    content, this.mods['type']: [
        {
            elem: 'title',
            content: BEM.I18N('menu', this.mods['type'])
        },
        {
            elem: 'list',
            content: this.ctx.content
        }
    ]
}

block menu, elem list {
    tag: 'ul'
}
