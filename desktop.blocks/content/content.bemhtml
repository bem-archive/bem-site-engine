block content {

    default: {
        applyCtx({
            block : 'body',
            mix : {
                block : 'layout',
                mods : { section : '2-1' }
            },
            content : this.ctx
        })
    }

    mix: { block : 'layout', elem : 'col' }

}
