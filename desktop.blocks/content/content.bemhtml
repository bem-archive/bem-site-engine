block content {

    default: {
        applyCtx({
            block : 'body',
            mix : {
                block : 'layout',
                mods : { section : 'center' }
            },
            content : this.ctx
        })
    }

    mix: { block : 'layout', elem : 'col' }

}
