block content {

    // default: {
    //     return applyCtx([
    //         { block : 'breadcrumbs' },
    //         this.ctx
    //     ]);
    // }

    content: {
        return apply('pager', this.ctx = this.data.page)
    }

    pager: {
        return { block : 'p-' + this.ctx };
    }

}
