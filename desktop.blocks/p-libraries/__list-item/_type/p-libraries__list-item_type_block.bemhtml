block p-libraries, elem list-item, elemMod type block {

    tag: 'a'

    attrs: { href : this.ctx.url }

    mix : { block: 'island' }

	content: {
        block : 'topic',
        mods : { type : 'block'},
        content : this.ctx
    }
}