block author, elem e-mail {

    default, !(this.ctx && this.ctx.content): {
        return false;
    }

    content: [
        BEM.I18N('author', this.elem),
        ': ',
        this.ctx.content
    ]
}
