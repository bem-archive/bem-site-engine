block author, elem name {

    default, !(this.ctx && this.ctx.content): {
        return false;
    }

    tag: 'span'

    content: {
        var c = this.ctx.content;

        return [
            c.lastName,
            ' ',
            c.firstName
        ]
    }
}
