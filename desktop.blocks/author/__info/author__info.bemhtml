block author, elem info {

    default, !(this.ctx && this.ctx.content): {
        return false;
    }

    content: [
        BEM.I18N('author', this.elem),
        ': ',
        {
            tag: 'strong',
            content: applyNext()
        }
    ]
}
