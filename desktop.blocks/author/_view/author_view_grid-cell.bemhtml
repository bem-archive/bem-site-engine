block author, mod view grid-cell {

    tag: 'a',

    attrs: { href: '/' + this.ctx.content.type + '/' + this.ctx.content.url }

    content: [
        {
            block: 'author',
            elem: 'avatar',
            elemMods: { size: 'medium' },
            content: this.ctx.content
        },
        {
            block: 'author',
            elem: 'name',
            content: this.ctx.content
        }
    ]
}
