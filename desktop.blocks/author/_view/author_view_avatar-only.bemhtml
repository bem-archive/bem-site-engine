block author, mod view avatar-only {

    content: [
        {
            block: 'author',
            elem: 'avatar',
            elemMods: this.ctx.content.avatar ? { size: 'small' } : { size: 'small', 'no-image': 'yes' },
            content: this.ctx.content
        }
    ]
}
