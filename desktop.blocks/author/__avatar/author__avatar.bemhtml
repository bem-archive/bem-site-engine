block author, elem avatar {

    this.ctx.content {
        this.ctx.content.avatar {
            tag: 'img'

            attrs: {
                src : this.ctx.content.avatar,
                alt : this.ctx.content.id
            }
        }
    }
}
