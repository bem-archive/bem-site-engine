block topic {

    default: {
        applyNext(this._url = this.ctx.url);
    }

    elem name {

        default: {
            applyCtx({
                block : 'link',
                url : this._url,
                mix : { block : this.block, elem : this.elem },
                content : this.ctx.content
            });
        }

    }

}