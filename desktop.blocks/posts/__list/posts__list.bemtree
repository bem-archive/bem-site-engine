block posts, elem list {

    content {

        this._.isArray(this.ctx.content): {
            var i = 0,
                buf = [],
                item;

            while(item = this.ctx.content[i]) {
                buf[i++] = {
                    block: 'posts-item',
                    content: item
                }
            }

            return buf;
        }
    }
}
