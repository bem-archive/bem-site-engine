block posts, elem list {

    content {

        this._.isArray(this.ctx.source): {

            return this.ctx.source.map(function(item){
                return apply(this._mode = '', this.ctx = {
                    block: 'post',
                    mods: this.mods,
                    source: item.id
                });
            });
        }
    }
}
