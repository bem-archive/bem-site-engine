block posts, elem footer {

    content: {
        return this.ctx.params.pager ? [
            apply(
                this._mode = '',
                this.ctx = {
                    block : 'pager',
                    params : this.ctx.params
                }
            )
        ] : [];
    }

}
