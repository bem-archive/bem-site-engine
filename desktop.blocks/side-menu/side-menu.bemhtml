block side-menu {

    mix: [
        { block : 'island' }//,
        //{ block : 'layout', elem : 'col', mods: {type: 'left'} }
    ]

    content, (this.ctx && this.ctx.type): [
        {
            elem: 'title',
            content: BEM.I18N('side-menu', this.ctx.type)
        },
        applyNext()
    ]
}
