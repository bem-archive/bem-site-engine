block post, elem title {

    content: {
        block: 'link',
        url: this.ctx.url,
        content: this.ctx.title
    }
}

block post, mod view full, elem title {

    content: {
        block: 'link',
        url: null,
        content: this.ctx.title
    }
}
