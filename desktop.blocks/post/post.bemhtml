block post {
    mix : { block : 'island' }
}

/*не выводим элемент content когда post в моде элемента списка*/
block post, mod display-type item, elem content, default: {
    return false;
}

block post, elem id, default: {
    return false;
}

block post, elem type, default: {
    return false;
}
