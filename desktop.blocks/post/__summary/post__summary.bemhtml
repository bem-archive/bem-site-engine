block post, elem summary {

    default, (this.ctx && this.ctx.content): {
        applyNext();
    }

    content : [
        BEM.I18N('post', this.elem),
        ': ',
        {
            tag: 'strong',
            content: this.ctx.content
        }
    ]
}
