block post, elem author {

    default, !(this.ctx && this.ctx.content): {
        return false;
    }

    content: [
        BEM.I18N('post', this.ctx.content.length > 1 ? 'authors' : 'author'),
        ': ',
        applyNext()
    ]
}
