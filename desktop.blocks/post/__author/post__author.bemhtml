block post, elem author {


    default, (this.ctx && this.ctx.content): {
        applyNext();
    }


    (this.ctx && this.ctx.content), content : [
        BEM.I18N('post', this.elem),
        ': ',
        {
            tag: 'strong',
            content: this.ctx.content
        }
        //TODO make autor name as url to other page
    ]
}
