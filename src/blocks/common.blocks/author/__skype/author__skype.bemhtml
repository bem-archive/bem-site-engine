block('author').elem('skype')(

    def()(match(function () { return !this.ctx.content; })(false)),

    content()(function () {
        return [
            {
                block: 'author',
                elem: 'label'
            },
            {
                block: 'author',
                elem: 'value',
                content: this.ctx.content
            }
        ];
    })
);
