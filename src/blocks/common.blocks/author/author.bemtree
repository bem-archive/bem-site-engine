block('author')(
    content()(function() {
        var id = this.ctx.id,
            author = this.data.people[id][this.data.lang] || id;
        return this.extend(author, { url: this.data.peopleUrls[id] });
    })
);
