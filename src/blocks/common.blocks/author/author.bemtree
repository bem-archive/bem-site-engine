block author, content: {

    var author = data.people.getPeople()[this.ctx.id][this.data.req.prefLocale] || this.ctx.id;

    return this._.extend(author, {
        url: model.getPeopleUrls()[this.ctx.id]
    });
}
