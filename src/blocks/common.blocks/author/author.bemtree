block author, content: {

    var author = leData.getPeople()[this.ctx.id][this.data.req.prefLocale] || this.ctx.id;

    return this._.extend(author, {
        url: leApp.getPeopleUrls()[this.ctx.id]
    });
}
