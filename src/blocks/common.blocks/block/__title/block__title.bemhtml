block('block').elem('title')(

    tag()('h1'),

    attrs()(function () {
        // The attribute is required to specify the search engine that this block's title
        return { 'data-search': 'title' };
    })
);
