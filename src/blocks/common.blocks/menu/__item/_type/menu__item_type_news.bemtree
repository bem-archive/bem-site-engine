block menu, elem item, elemMod type news {

    content: {

        return [
            {
                block: 'post',
                mods: { view: 'menu-item' },
                showDate: true,
                id: this.ctx.id,
                category: this.ctx.category,
                active: this.elemMods['active']
            }
        ]
    }
}
