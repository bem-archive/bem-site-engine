block menu {

    this.data.menu, content: {

        return this.data.menu.map(function(column, index) {
            return {
                block: 'menu-list',
                mods: { main: index === 0 },
                content: column
            }
        });
    }
}
