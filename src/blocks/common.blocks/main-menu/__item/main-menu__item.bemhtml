block main-menu, elem item {

    content: [
        {
            block : 'link',
            mods : {type : 'block'},
            url   : this.ctx.url,
            content : BEM.I18N('main-menu', this.ctx.name)
        }
    ]
}
