block('page').elem('css')(
    def()(function () {
        var elem = this.elem;

        return [
            { elem: elem, url: this._pathToBundle + '.css', ie: false },
            { elem: elem, url: this._pathToBundle + '.ie.css', ie: 'lte IE 9' }
        ];
    })
);
