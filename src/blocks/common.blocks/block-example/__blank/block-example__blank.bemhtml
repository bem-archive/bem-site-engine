block('block-example').elem('blank')(
    def()(function () {
        var ctx = this.ctx;

        return applyCtx({
            block: 'link',
            url: ctx.url,
            attrs: ctx.attrs,
            mix: {
                block: this.block,
                elem: 'link',
                elemMods: { icon: 'blank' }
            },
            content: ctx.content
        });
    })
);
