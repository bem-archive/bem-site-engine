block('block-example').elem('source-switcher')(
    content()(function() {
        return 'BEMJSON';
    }),

    mix()(function() {
        return {
            block: this.block,
            elem: 'link',
            elemMods: { icon: 'source' }
        }
    })
);
