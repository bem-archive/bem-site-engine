block menu-list {

    this.ctx.content, content: {

        return this.ctx.content.map(function(item) {
            return {
                elem: 'item',
                title: item.title,
                url: item.url,
                elemMods: {
                    active: item.active,
                    type: item.type
                }
            };
        });
    }
}