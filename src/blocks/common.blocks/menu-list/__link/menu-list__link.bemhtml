block('menu-list').elem('link')(
    def()(function() {
        return applyCtx({
            block: 'link',
            url: this.ctx.url,
            content: this.ctx.title,
            mix: [{
                block: 'menu-list',
                elem: 'link',
                elemMods: this.ctx.elemMods
            }]
        });
    })
);
