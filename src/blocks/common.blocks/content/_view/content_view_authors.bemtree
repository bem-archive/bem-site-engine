block content, mod view authors {

    content: this.data.authors.map(function(author) {
        return {
            block: 'author',
            mods: { view: 'grid-cell' },
            id: author
        }
    })
}
