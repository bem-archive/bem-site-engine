block content, mod view authors {

    content: [
        applyNext(),
        {
            elem: 'content-area',
            content: data.docs.getAuthors().map(function(author) {
                return {
                    block: 'author',
                    mods: { view: 'grid-cell' },
                    id: author
                }
            })
        }
    ]
}