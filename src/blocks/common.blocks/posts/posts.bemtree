block('posts').content()(function () {
    return {
        var node = this.ctx.node;

        if(!node.items) {
            return [];
        }

        return {
            elem: 'list',
            content: node.items
        };
    }
});
