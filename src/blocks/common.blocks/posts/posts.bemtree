block posts {

    content: {
        var node = this.data.node;

        if(!node.items) {
            return [];
        }

        return {
            elem: 'list',
            content: node.items
        };
    }
}
