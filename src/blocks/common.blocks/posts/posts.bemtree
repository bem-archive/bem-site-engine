block('posts')(

    content()(
        (function() {
            return {
                elem: 'list',
                content: this.ctx.node
            };
        }),

        match(function() {
            var node = this.ctx.node;
            return !node.items || !node.items.length;
        })(function() {
            return [];
        })
    )
)
