block posts {

    content: {
        var node = this.ctx.node;

        if(!node.items) {
            return [];
        }

        return {
            elem: 'list',
            content: node.items
        };
    }
}
